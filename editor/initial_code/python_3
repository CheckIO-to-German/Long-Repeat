def long_repeat(line):
    """
        Return the length of the longest substring that consists of only one letter.
    """
    # your code here
    return 0

if __name__ == '__main__':
    # "asserts" are for self-checking only and are not necessary for auto-testing
    assert long_repeat('sdsffffse') == 4, "First"
    assert long_repeat('ddvvrwwwrggg') == 3, "Second"
    print('"Run" is good. How is "Check"?')
